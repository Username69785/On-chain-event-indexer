//! Data types for working with signatures.
//!
//! Contains struct definitions that reflect Helius API responses when
//! querying transaction signatures.

// TODO:
// - Signature struct (single signature from the response: contains the signature, slot, block time, err)
// - SignaturesResponse struct (array of signatures or an RPC response wrapper)
